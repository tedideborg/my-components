import { html } from 'https://esm.sh/@arrow-js/core';
import codeBlock from '../components/codeBlock.js';

const templatesPage = () => {
    return html`
        <section>
            <hgroup>
                <h3>Games template</h3>
                <h4>
                    A basic starter template for developing games with excalibur
                    js
                </h4>
            </hgroup>

            <a>A link to a template</a>
        </section>
    `;
};

export default templatesPage;
